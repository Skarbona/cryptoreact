import React from 'react';

const PriceNull = () => {
    return (
        <div>
            <h1>Select and Save favourites to see items in Dashboard!</h1>
        </div>
    );
};

export default PriceNull;
